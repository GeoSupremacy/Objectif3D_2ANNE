#include "Composite.h"
/*
void Composite::DrawPrice()
{
	
	
	
	std::cout << "Composite: " << value << std::endl;
}

void Composite::ReturnPrice()
{
	for (Composite _composite : composites)
		value += _composite.GetValue();
	
	for (Leaf _leaf : leafs)
		value += _leaf.GetValue();
}
*/