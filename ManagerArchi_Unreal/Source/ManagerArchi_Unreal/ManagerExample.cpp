#include "ManagerExample.h"
/*
void AManagerExample::Add(AItemManaged& _item)
{
    if (items.count(_item.GetID()))
        return;
    items.insert({ _item.GetID(), _item });
   // _item.SetNetDriverName(_item.GetFName()"[MaNAGED]");
   
}

void AManagerExample::Remove(AItemManaged& _item)
{
    
    if (!items.count(_item.GetID()))
        return;
    items.erase(_item.GetID());
    
}
*/